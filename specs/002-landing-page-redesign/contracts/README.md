# API Contracts: Landing Page Redesign

No API contracts for this feature. The landing page is purely presentational with no new endpoints, no data fetching, and no API changes.

Existing contracts remain unchanged:
- `GET /results?q={query}&mode=browse` — triggered by the search bar (existing)
- `/quiz` — linked via CTA buttons (existing route, no API)
